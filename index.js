'use strict';

const trelloBoardController = (function () {})();

const viewController = (function () {})();

const controller = (function () {
  return {
    init: function () {
      console.log('Application has started');
    },
  };
})();
controller.init();
